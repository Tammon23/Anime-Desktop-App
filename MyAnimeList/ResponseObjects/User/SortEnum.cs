namespace MyAnimeList.ResponseObjects.User
{
    namespace MyAnimeList.ResponseObjects.User
    {
        public enum SortEnum
        {
            ListScore,
            ListUpdatedAt,
            AnimeTitle,
            AnimeStartDate,
            AnimeId
        }
    }
}